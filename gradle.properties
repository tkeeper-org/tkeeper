kotlin.code.style=official

# Runtime feature modules for the root application build.
# Examples:
#   ./gradlew shadowJar -Pkeeper.features=agentic-payments,digital-assets -Pkeeper.platforms=ecc
#   ./gradlew dockerBuild -Pkeeper.docker.features=digital-assets,seal-aws -Pkeeper.docker.platforms=ecc
#   ./gradlew shadowJar -Pkeeper.features=all -Pkeeper.platforms=all
#   ./gradlew shadowJar -Pkeeper.feature.ecies=true -Pkeeper.platform.ecc=true
keeper.features=
keeper.platforms=
# keeper.docker.features=
# keeper.docker.platforms=
# keeper.features.all=false
# keeper.platforms.all=false
# keeper.platform.ecc=false
# keeper.feature.seal.aws=false
# keeper.feature.seal.gcloud=false
# keeper.feature.digital-assets.bitcoin=false
# keeper.feature.digital-assets.evm=false
# keeper.feature.digital-assets.tron=false
# keeper.feature.digital-assets.xrp=false
# keeper.feature.digital-assets.solana=false
# keeper.feature.authority.x509=false
# keeper.feature.ecies=false
# keeper.feature.auth.dev=false
# keeper.feature.agentic-payments.ap2=false
# keeper.feature.agentic-payments.mc-vi=false
